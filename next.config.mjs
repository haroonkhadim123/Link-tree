/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.facebook.com", "www.instagram.com", "cdn.example.com"], // add your hosts
  },
};

export default nextConfig;
